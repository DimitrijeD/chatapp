<!DOCTYPE html>
<html>
<head>
    <title>Email Validation for {{ config('app.name') }}</title>
</head>
<body>
    <p>User: {{ $first_name }} {{ $first_name }}</p>
    <a href="{{ $url }}">Follow this link to validate your account</a>

    <br>
    <p>{{ $url }}</p>
</body>
</html>
